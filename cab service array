#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,n1,n2;
    scanf("%d%d%d",&n,&n1,&n2);
    int a[n],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a[i]>n1 && a[i]<n2)
        {
         printf("%d ",a[i]);   
        }
    }

    return 0;
}
