#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,i,p,pi;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    scanf("%d",&p);
    if(p>n)
    {
        printf("Invalid Input");
    }
    else
    {
        scanf("%d",&pi);
        for(i=0;i<p-1;i++)
        {
            printf("%d\n",a[i]);
        }
        for(i=p;i<n;i++){
            printf("%d\n",a[i]);
        }
    }
